const [min, max] = [5,30];

/* if (min < max) {
    console.log("Min lower than Max");
} else {
    console.log("Min is not lower");
}; */

// min < max ? console.log("Min lower than Max") : console.log("Min is not lower");

console.log(`Min ${min < max ? "" : "NOT"} lower than Max`);